# Whisper Models Directory

This directory holds whisper.cpp model files (`.bin` format) used by the Hands-Free Bible feature.

The `small.en` model (~466 MB) is downloaded automatically on first launch.

**Do not commit model files to Git** — they are downloaded at runtime and excluded via `.gitignore`.
